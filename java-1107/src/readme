静态变量：被static修饰的变量
    被类拥有
    所有实例共享
    可通过类名对象名访问

静态方法：被static修饰
    静态方法，静态代码块中不能访问实例变量

类加载顺序
    静态变量初始化<静态方法初始化<静态代码块初始化<

常量：被关键字final修饰
    必须显示的赋初始值
    一旦赋值不能改变
    通常和static一起使用

包：
    可以将不同功能的类放在不同的包中进行区分
    解决类名重复的问题
    通常以域名反写命名