类：同种对象的集合与抽象，是创建对象的模板
对象：类的具体实例化结果

对象的创建：
    类名 对象名 = new 类名（）

java中的四种方法：
   无参数无返回值：只执行方法体
   无参数有返回值：执行完方法后，将数据结果返回，调用完方法后接收返回值
   有参数无返回值：调用方法时需提供实际参数值
   有参数有返回值：调用方法时需提供实际参数值，调用完方法后接收返回值

形式参数
实际参数：调用方法时，实际传递的数据

构造方法：
   用于初始化对象内部状态的特殊方法
   方法名必须和类名一样
   无返回值也无需加void


方法的重载：
   同一个类中
   方法名相同
